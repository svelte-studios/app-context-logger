{
  "name": "app-context-logger",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/svelte-studios/app-context-logger.git",
  "author": "Svelte Studios <info@sveltestudios.com>",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "release": "yarn lint && yarn test && standard-version && git push --follow-tags && npm publish",
    "test": "yarn lint && jest"
  },
  "dependencies": {
    "debug": "^4.3.1"
  },
  "devDependencies": {
    "husky": "^5.0.6"
  }
}
